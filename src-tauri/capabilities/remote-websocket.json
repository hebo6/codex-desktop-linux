{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-websocket",
  "description": "Allows application windows to interact with an owned remote WebSocket app-server connection",
  "windows": ["main", "app-*"],
  "permissions": [
    "allow-send-remote-websocket-message",
    "allow-disconnect-remote-websocket"
  ]
}
