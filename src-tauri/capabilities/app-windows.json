{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app-windows",
  "description": "Allows application windows to manage their own state and create a server-bound window",
  "windows": ["main", "app-*"],
  "permissions": ["allow-open-app-window"]
}
